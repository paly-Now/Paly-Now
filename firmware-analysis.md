# Play Now – Firmware Analysis

تم تحليل ملف الـFirmware المرفوع:
`DUO_888_1506TV_512M_8M_SCB5_internal_54650_20231227_180037.bin`

## النتيجة
- الحجم: 6,758,400 bytes (~6.45 MB).
- يتضمن كود MIPS/bootloader واضحًا لجهاز Sunplus 1506F، مع نصوص مثل `zeroBoot v1.0 for 1506F` و`Boot form SPI v1.13`.
- لم يتم العثور على URL صالح للبث أو Playlist أو بيانات Xtream/Portal قابلة للاستخدام.
- ظهرت كلمتا `M3U` و`m3u` داخل بيانات ثنائية عالية العشوائية، لكن دون بنية Playlist أو رابط بث حولها؛ لذلك لم يتم اعتبارها مصدر IPTV.
- يوجد نص TFTP اختباري (`tftp://cdtftp01/user name/rom_net_upgrade.bin`) وهو خاص بالتحديث/البيئة الداخلية وليس مصدر IPTV، ولذلك لم يتم ربطه كقناة بث.

## ما تم تنفيذه في Play Now V4
- إضافة قسم `LIVE` مخفي عن المستخدمين افتراضيًا.
- التفعيل من لوحة الإدارة فقط.
- دعم إعداد مصدر IPTV بصيغ M3U/M3U8 وXtream وPortal.
- استيراد ملف M3U وتحويله لقنوات.
- اختبار مصدر M3U من الإدارة مع مراعاة CORS.
- إدارة أسماء القنوات والشعارات والتصنيفات والروابط.
- مشغل Live داخل الموقع.

**لا يوجد ربط مباشر بسيرفر Nova من هذا الـFirmware لأن عنوان/واجهة السيرفر غير موجودين بصيغة قابلة للاستخدام داخل الملف.**
